# DUETTrack 论文架构图复用提示词

请按照以下固定规范绘制或修改我的论文架构图。先理解论文方法和每个模块的真实数据流，再构图；不要根据模块名称自行补充不存在的机制。

## 视觉风格

- 参考 MTUTrack 的学术图形语言：白色背景、低饱和度柔和配色、细边框、紧凑留白、清楚的阅读顺序；只参考风格，不照抄其构图。
- 使用衬线字体；标题层级明确，但不要用超大标题。
- 颜色保持语义一致：蓝色表示状态/运动，橙色表示外观/ReID，紫色表示时序或跨证据建模，绿色表示策略/监督目标，青色表示可靠性证据。
- 不使用渐变、阴影、立体效果、发光效果、装饰性图标或过度圆角，避免明显的 AI 海报感和工程流程图感。

## 构图与连线

- 所有流程箭头必须横平竖直，只能使用水平段和垂直段；禁止斜箭头。
- 一对多分支先连接公共水平干线，再分别垂直进入模块；多对一先汇入公共干线，再用一根箭头进入目标。
- 尽量避免交叉线、回头线和贴着文字经过的线；跨区域反馈放在外侧边缘。
- 主流程从左到右或从上到下，读者不看正文也能判断输入、处理和输出。
- 同层模块大小、间距、边框粗细和箭头大小保持一致。

## 文字与学术表达

- 每个框只表达一个概念，优先使用 1--4 个英文单词；不要把解释性句子塞进框内。
- 图中只保留必要的变量符号和维度；推导、损失定义、阈值、限幅公式和实现超参数放在正文或图注。
- 总览图强调时序证据、反事实效用估计、跨证据校准和双状态同化等研究概念，不使用 `controller`、`bounded`、`clip` 等偏工程实现的标题。
- 缩写第一次出现时应能从图注或相邻短语理解；不要堆叠术语。

## DUETTrack 四张图的固定内容

### 图1：研究动机对照

- 删除额外的时序流程或方法内部结构，只保留左右对照。
- 左侧为 Conventional trackers：接受观测后直接进行 Motion update 与 Appearance update，不使用强度时钟。
- 右侧为 DUETTrack：保留两只时钟式强度符号，分别表达 Motion strength 与 Appearance strength，填充比例可以不同。
- 两侧统一使用 Motion state 与 Appearance memory 作为输出，并保持位置、颜色和尺寸严格对齐。
- 图1只表达“固定全量同化”与“独立自适应同化”的概念差异；CUE、CEC、因果记忆和具体数据流留给图2--4。

### 图2：整体框架

- 分为三个阶段：Causal event memory、Temporal utility estimation、Dual-state assimilation。
- 左上保留真实连续视频帧，标记为过去到当前时刻，用真实图像体现时序输入。
- 当前 TrackTrack 匹配对在状态-关联与 ReID 交互两个层面形成事件，并写入六事件因果记忆。
- CUE 与 CEC 画成两个独立、紧凑的学术模块。
- 最终运动和外观系数分别控制 Kalman 状态与 EMA 原型的同化。

### 图3：CUE

- 左侧为 offline counterfactual teacher：共享前状态，分别比较 WRITE 与 HOLD，并在相同未来证据下产生监督目标。
- 右侧为 online causal estimator：六个因果事件分别编码状态线索与 ReID 交互，再进行事件融合和因果时序建模。
- 输出保持简洁：Policy、Offset 汇聚为 Base gates；Confidence、Risk 汇聚为 CEC evidence。
- 不在框内展开基础系数公式。

### 图4：CEC

- 分为 Evidence encoding、Three-token interaction、State-specific calibration 三部分。
- 状态-关联历史、外观交互历史和可靠性上下文分别形成三个 token，再进行跨证据交互。
- 两条输出支路分别对应 KF 与 EMA 校准，保持上下对称。
- 详细约束公式由正文说明，图中突出证据如何进入两种状态的校准过程。

## 真实图像

- 若使用视频帧，应选择同一序列的连续或有明确时间顺序的帧，并保留真实检测框/轨迹框。
- 图片只承担输入语境、时序变化或案例证据，不作为装饰背景。
- 图片裁剪比例、边框和间距保持一致，当前帧可以用略深边框强调。

## 交付流程

1. 先通读论文相关方法段落，列出图中必须出现的数据流。
2. 先输出三张高清预览，不插入论文。
3. 自检文字溢出、箭头交叉、斜箭头、空白失衡、配色不一致和模块语义错误。
4. 等我明确确认后，再替换 LaTeX 中的原图并编译整篇论文。
5. 最终提供高清 PNG、矢量 PDF/SVG（可生成时）以及完整 LaTeX ZIP。

一句话版本：请以 MTUTrack 的克制学术风格重绘 DUETTrack 四幅图，使用低饱和语义配色、短标签和严格正交箭头；图1以左右对照和时钟式强度符号呈现核心动机，图2用真实帧体现时序输入，公式与实现约束移至正文，先给预览，确认后再插入论文。
